import { html } from '../../node_modules/lit-html/lit-html.js';

// Todo... Replace with actual layout
export const layoutTemplate = (userData, content) => html`
    <header>
        <!-- REPLACE WITH ACTUAL NAV -->
    </header>
    <main>
        ${content}
    </main>
`;

